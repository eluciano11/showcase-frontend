import {
  transformDate
} from 'showcase-frontend/helpers/transform-date';

module('TransformDateHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = transformDate(42);
  ok(result);
});
